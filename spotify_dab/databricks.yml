# This is a Declarative Automation Bundle definition for spotify_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: spotify_dab
  uuid: 0384558c-4456-4721-af8f-446431364e0b

include:
  - resources/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405606215515780.0.azuredatabricks.net
      root_path: /Workspace/Users/harshitgarg0834_gmail.com#ext#@harshitgarg0834gmail.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    presets:
      source_linked_deployment: false
    variables:
      catalog: spotify_cata
      schema: dev
  prod:
    mode: production
    workspace:
      host: http://127.0.0.1:7073
      # We explicitly deploy to /Workspace/Users/harshitgarg0834_gmail.com#ext#@harshitgarg0834gmail.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/harshitgarg0834_gmail.com#ext#@harshitgarg0834gmail.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: azuredatabricks
      schema: prod
    permissions:
      - user_name: harshitgarg0834_gmail.com#ext#@harshitgarg0834gmail.onmicrosoft.com
        level: CAN_MANAGE
